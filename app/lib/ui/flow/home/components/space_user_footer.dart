import 'package:flutter/material.dart';

class SpaceUserFooter extends StatelessWidget {
  const SpaceUserFooter({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
